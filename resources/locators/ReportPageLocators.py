"""Locators — Trang Báo cáo & Thống kê (/reports)."""

REP_EXPORT_EXCEL = "xpath=//button[normalize-space()='Xuất Excel']"
REP_EXPORT_PDF = "xpath=//button[normalize-space()='Xuất PDF']"
# Trigger CustomSelect hiển thị theo giá trị mặc định khi mới mở trang.
REP_TYPE_SELECT = "xpath=//button[.//span[normalize-space()='Tổng hợp tồn kho']]"
REP_PERIOD_SELECT = "xpath=//button[.//span[normalize-space()='Tháng này']]"
